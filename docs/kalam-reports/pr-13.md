=== KALAM REPORT ===
engine: 3fcfb91 (bundle on my branch as b6742e3)
step: item 4 — redone as a pull request, after the owner's correction
status: PR OPEN — #13, waiting on the owner to merge it

Correction to `merge-to-main.md`: item 4 was first done by pushing a merge
commit straight to `main` (that commit was `e4771d6`, parents `4628dbf` and
the branch tip). The owner objected, correctly — this repository's
convention is pull requests, and PRs #8–12 were all merged through GitHub —
so the direct push has been undone and the merge is now a pull request the
owner reviews and merges:

    PR #13  "The kalam-reader migration: WebKit out, the engine widget in"
    https://github.com/alankarter1275/calibre-alt/pull/13
    arena/01a08cfb-calibre-alt -> main
    state: OPEN, mergeable: MERGEABLE, 58 files, +5638 / -5772

Why a PR was impossible at first, because it is worth knowing before the
next round: `main` was a **parentless snapshot** (`4628dbf`) sharing no
history with this branch, and GitHub refuses to open a PR between unrelated
histories — its own words, from the attempt I made to be sure rather than
guess: "The test-unrelated-1 branch has no history in common with main".
The merge commits behind PRs #8–12 are not in this repository either (I
asked GitHub for three of them by hash; all three are absent), so this
checkout is a re-created snapshot that kept the PR records and not the
history under them.

What was done to make the PR possible: `main` was moved to `c652a31` — this
branch's own root, the pre-migration state, which is the same content as the
old tip apart from its `Cargo.lock` and a few doc/log files. That was a
force-push (the first anywhere in this work) and nothing was lost: the old
tip `506e4ef` is recorded and recoverable, and the tree reverts to the
pre-migration reader, which is exactly what a PR needs as its base. The
diff the owner now reviews is the 153 commits of the migration, not the
whole repository.

Merge instruction carried into the PR body: **create a merge commit, no
squash, no rebase**, and the merged tree must be the branch's — in
particular `src/webview_pool.rs` and `docs/engine-handoff/patch/engine.rs`
stay deleted.

Branch: kept. The `tag = "v0.1.0"` line you will send needs somewhere to
land, and it is cheaper to land it here than to re-open a deleted branch.
Say the word when the tag exists and it goes in as one commit, then the
branch can go.

Follow-up, the same hour: main moved under the PR. The `pull_request`
workflow and the workflow on main are the same file, so the force-push to
main started a run there, and that run's own publish steps committed a
normalized `Cargo.lock` and a clippy log to main — which is what turned the
PR `CONFLICTING`, since the branch edits those same two files. Resolved the
ordinary way: `main` merged into the branch, both conflicts taken from the
branch (its lock is the one CI built and it pins `kalam-reader` at
3fcfb91), and the merge commit adds no content of its own — the tree is
identical to the branch tip. PR is `MERGEABLE` / `CLEAN` again, and because
main is now an ancestor of the branch, the merge GitHub performs will
produce exactly the branch's tree.

For the record, main's own CI run failed — `c652a31` by itself never passed
CI (it is the pre-migration state, mid-migration code with no kalam-reader
dependency yet). That is why the run there published a failures file rather
than a report. It does not affect the PR: GitHub builds the pull request's
merge result and checks that, which is the tree CI has been passing on the
branch all along.

Both CI and CodeRabbit have started on the PR; the workflow's publish steps
push to `$GITHUB_REF_NAME`, which on a `pull_request` event is `N/merge`, so
those pushes will fail harmlessly there — the logs still land in the Actions
tab. Noting it so it is not mistaken for a broken job later.
=== END ===
